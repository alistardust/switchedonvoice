# SwitchedOnVoice

Real-time voice feminization training — pitch, resonance, and gamification.

## Requirements
- Python 3.11+
- A microphone

## Install
pip install uv
uv pip install -e ".[dev]"

## Run
python run.py
