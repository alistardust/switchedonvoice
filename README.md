# SwitchedOnVoice

Real-time voice feminization training — pitch, resonance, and gamification.

## Requirements
- Python 3.11+
- A microphone

## Install
```bash
pip install uv
uv pip install -e ".[dev]"
```

## Run
```bash
python run.py
```
